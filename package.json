{
  "name": "persona-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Chat with AI personas of Hitesh Choudhary and Piyush Garg (stylistic simulation) — PERN + Neon + Vercel + OpenAI.",
  "main": "server/index.js",
  "scripts": {
    "dev:server": "nodemon server/index.js",
    "start": "node server/index.js",
    "dev:client": "npm --prefix client run dev",
    "build:client": "npm --prefix client install && npm --prefix client run build",
    "db:migrate": "node server/db/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.55.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18"
  }
}
